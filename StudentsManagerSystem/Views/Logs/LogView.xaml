<Page x:Class="StudentsManagerSystem.Views.Logs.LogView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="系统日志查看">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
            <Button Content="刷新" Width="90" Height="32" Click="btnRefresh_Click"/>
            <TextBlock Text="显示当前日志最近 500 行，日志超过 1MB 自动轮转。"
                       VerticalAlignment="Center"
                       Margin="12,0,0,0"
                       Foreground="#7F8C8D"/>
        </StackPanel>

        <ListBox x:Name="lstLogFiles" Grid.Row="1" Height="90" Margin="0,0,0,12"/>

        <TextBox x:Name="txtLogContent"
                 Grid.Row="2"
                 IsReadOnly="True"
                 AcceptsReturn="True"
                 AcceptsTab="True"
                 FontFamily="Consolas"
                 FontSize="13"
                 HorizontalScrollBarVisibility="Auto"
                 VerticalScrollBarVisibility="Auto"
                 TextWrapping="NoWrap"/>
    </Grid>
</Page>
