<Page x:Class="StudentsManagerSystem.Views.Score.ScoreView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:StudentsManagerSystem.Views.Score"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="1100">

    <Page.Resources>
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3498DB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="20,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2980B9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DeleteButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Background" Value="#E74C3C"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#C0392B"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Grid Background="White" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 筛选条件 -->
        <Border Grid.Row="0" Background="#F8F9FA" Padding="15" CornerRadius="5" Margin="0,0,0,15">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="学年：" VerticalAlignment="Center" FontSize="14" Margin="0,0,10,0"/>
                <ComboBox x:Name="cmbAcademicYear" Width="150" Height="32" FontSize="14">
                    <ComboBoxItem Content="2024-2025" IsSelected="True"/>
                    <ComboBoxItem Content="2023-2024"/>
                    <ComboBoxItem Content="2022-2023"/>
                </ComboBox>
                
                <TextBlock Text="学期：" VerticalAlignment="Center" FontSize="14" Margin="20,0,10,0"/>
                <ComboBox x:Name="cmbSemester" Width="120" Height="32" FontSize="14">
                    <ComboBoxItem Content="第一学期" IsSelected="True"/>
                    <ComboBoxItem Content="第二学期"/>
                </ComboBox>
                
                <Button Content="查询" Style="{StaticResource ButtonStyle}" Margin="20,5,5,5" Click="btnQuery_Click"/>
            </StackPanel>
        </Border>

        <!-- 操作按钮区 -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <Button Content="录入成绩" Style="{StaticResource ButtonStyle}" Click="btnAdd_Click"/>
            <Button Content="修改成绩" Style="{StaticResource ButtonStyle}" Click="btnEdit_Click"/>
            <Button Content="删除" Style="{StaticResource DeleteButtonStyle}" Click="btnDelete_Click"/>
            <Button Content="查看详情" Style="{StaticResource ButtonStyle}" Click="btnView_Click"/>
            <Button Content="导入成绩" Style="{StaticResource ButtonStyle}" Click="btnImport_Click"/>
            <Button Content="导出成绩" Style="{StaticResource ButtonStyle}" Click="btnExport_Click"/>
            <Button Content="刷新" Style="{StaticResource ButtonStyle}" Click="btnRefresh_Click"/>
        </StackPanel>

        <!-- 数据列表 -->
        <DataGrid x:Name="dataGrid" Grid.Row="2" 
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  AlternatingRowBackground="#F5F5F5"
                  RowHeight="35"
                  FontSize="13">
            <DataGrid.Columns>
                <DataGridTextColumn Header="学号" Binding="{Binding StudentNo}" Width="100"/>
                <DataGridTextColumn Header="姓名" Binding="{Binding StudentName}" Width="100"/>
                <DataGridTextColumn Header="学年" Binding="{Binding AcademicYear}" Width="100"/>
                <DataGridTextColumn Header="学期" Binding="{Binding Semester}" Width="100"/>
                <DataGridTextColumn Header="课程编号" Binding="{Binding CourseNo}" Width="100"/>
                <DataGridTextColumn Header="课程名称" Binding="{Binding CourseName}" Width="150"/>
                <DataGridTextColumn Header="学分" Binding="{Binding Credit}" Width="80"/>
                <DataGridTextColumn Header="平时成绩" Binding="{Binding RegularScore}" Width="100"/>
                <DataGridTextColumn Header="考试成绩" Binding="{Binding ExamScore}" Width="100"/>
                <DataGridTextColumn Header="总评成绩" Binding="{Binding TotalScore}" Width="100"/>
                <DataGridTextColumn Header="等级" Binding="{Binding Grade}" Width="80"/>
                <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
